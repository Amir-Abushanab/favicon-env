{
  "$schema": "./node_modules/knip/schema.json",
  // Entries (index/ssr/global) are auto-detected from package.json `exports` and
  // tsup.config.ts. The project scope is `src/` only — the tests import the built
  // `dist/` output (the published artifact), not `src/`, so they're out of scope.
  "project": ["src/**/*.ts"]
}
