name: Deploy demo to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4 # reads the pinned pnpm from package.json
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      # Mirror the repo layout so the demo's runtime imports/fetches resolve:
      #   /            → redirects to /examples/
      #   /examples/   → the demo (imports ../dist → /dist)
      #   /dist/       → the freshly built package
      #   /skills/     → the agent skill doc the "Copy for your agent" button fetches
      - name: Assemble site
        run: |
          mkdir -p _site/dist _site/examples _site/skills/core
          cp -r dist/. _site/dist/
          cp examples/index.html _site/examples/index.html
          cp skills/core/SKILL.md _site/skills/core/SKILL.md
          printf '<!doctype html><meta http-equiv="refresh" content="0; url=./examples/">\n' > _site/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deploy
        uses: actions/deploy-pages@v4
